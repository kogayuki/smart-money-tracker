app = "smart-money-tracker"
primary_region = "nrt"

[build]

[env]
  NODE_ENV = "production"
  LOG_LEVEL = "info"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"

[deploy]
  strategy = "rolling"

[processes]
  app = "node dist/index.js"
